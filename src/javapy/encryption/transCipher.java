package javapy.encryption;

public class transCipher {

	private char[] encodeEngine(String input) {

		// Do all encoding here. Have the public statements as accesspoints to this engine.

		return null;
	}

	public String encode(String input) {

		// Create a 2D array of letters. Use square root methods.
		// Make sure the array can hold the characters.
		// Plot the characters to a spot on the array.
		// Read the characters in a vertical form instead of normal horizontal.
		// Spit out the characters in a single string.

		// This is provisionary.
		encodeEngine(input);

		String output = null;
		return output;

	}

	public char[] encodeArray(String input) {
		return encodeEngine(input);
	}
}
