package javapy.graphics;

import java.io.File;

public class GraphicsEngine {

	/**
	 * Constructor calls a file called physics. It reads the file and find some data from that to define some stuff.
	 * 
	 * @param physics
	 *            - system for defining some projectivel motion parameters.
	 */
	GraphicsEngine(File physics) {
		// Create a system to model projectile motion and velocities. Lots of physics maths will be necessary.
	}
}
